__pycache__/
*.py[cod]
*.pyc
.DS_Store
*.bak
data/helm.db
data/*.db
_ic_*
_p*_*
_plc_*
_pm_*

# paper-book source modules are tracked source, not scratch
!_paper_*.py
*.log
.env
debit_spread_*.py
straddle_*.py

# editor/transfer backups
*.pre_*
*_bak

# sqlite wal/shm sidecars
*.db-shm
*.db-wal

# stray empty root db stub (real db is data/helm.db)
/helm.db

# editor/transfer backups
*.pre_*
*_bak

# sqlite wal/shm sidecars
*.db-shm
*.db-wal

# stray empty root db stub (real db is data/helm.db)
/helm.db

# base64 transfer crumbs
_bps_*

# regenerated grid render output
/master.html
/master_data.json

# local scratch / data archive (not tracked)
archive/
/data/master.csv
